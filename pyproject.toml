[project]
name = "neem-interface-python"
version = "1.0"
dependencies = [
    "roslibpy~=1.2.1",
    "tqdm",
    "scipy"
]

[build-system]
requires      = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"
